# UnityShaderNav — Handoff (2026-05-22)

切换机器/会话时读这一份就够。

## 项目当前阶段

**规划完成、实施未开始。** 仓库里只有 spec / ADR / context / 13 份 plan / 1 份 plan review。

## 仓库速览

```
agents-a20238c159/
├── README.md
├── LICENSE
├── CONTEXT.md                      # 术语表
├── UnityShaderNav_Spec.md          # 技术 spec（445 行）
├── docs/
│   ├── adr/
│   │   ├── 0001-multi-candidate-peek-for-ambiguous-symbols.md
│   │   ├── 0002-manifest-driven-package-indexing.md
│   │   ├── 0003-macro-pattern-whitelist.md
│   │   └── 0004-persist-index-cache-under-library.md
│   └── superpowers/plans/          # ← 本轮新增
│       ├── README.md               # 13 个 plan 的索引 + 依赖图
│       ├── 2026-05-22-01-project-scaffolding.md
│       ├── 2026-05-22-02-shaderlab-block-parser.md
│       ├── 2026-05-22-03-hlsl-symbol-collector.md
│       ├── 2026-05-22-04-single-file-definition.md
│       ├── 2026-05-22-05-macro-pattern-recognizer.md
│       ├── 2026-05-22-06-include-resolver.md
│       ├── 2026-05-22-07-package-resolver-and-cross-file.md
│       ├── 2026-05-22-08-index-lifecycle.md
│       ├── 2026-05-22-09-cache-persistence.md
│       ├── 2026-05-22-10-document-symbols.md
│       ├── 2026-05-22-11-chain-lookup.md
│       ├── 2026-05-22-12-macro-definitions.md
│       ├── 2026-05-22-13-find-references.md
│       └── REVIEW-2026-05-22.md    # 两轮独立 review 的合并清单
```

无源代码、无 `package.json`、无 `node_modules`。

## 这次会话产出

1. **13 个 plan 文档**，按 spec §10 的验收用例和 MVP→P1 进度切分。Plan 01-07 是 MVP，Plan 08-13 是 P1 + 基础设施。每个 plan 含 Goal / Architecture / Dependencies / File Structure / 一组 Task（每 Task 5-8 个 checkbox 步骤，含可执行代码和命令）/ Acceptance / Manual Verification。
2. **`docs/superpowers/plans/README.md`** ：索引页 + 依赖图 + 通用约定（TDD、conventional commits、fixture 位置、类型一致性纪律）。
3. **`docs/superpowers/plans/REVIEW-2026-05-22.md`** ：两个独立 review 子代理跑出来的合并清单 —— 10 个 🔴 Blocker / 9 个 🟡 Risk / 7 个 🟢 Polish。这份是恢复工作时必须先看的。

## 计划总览（13 个里程碑）

| # | 计划 | 里程碑 | Spec §10 验收 |
|---|---|---|---|
| 01 | project-scaffolding | LSP 扩展激活、握手通过 | — |
| 02 | shaderlab-block-parser | `.shader` HLSL 块边界扫描 | — |
| 03 | hlsl-symbol-collector | tree-sitter 提取符号到 FileIndex | — |
| 04 | single-file-definition | F12 同文件可用 | Case 1, 8 |
| 05 | macro-pattern-recognizer | `TEXTURE2D` / `#pragma vertex` | Case 5, 6, 7 |
| 06 | include-resolver | F12 在 `#include` 上 | Case 4 |
| 07 | package-resolver-and-cross-file | 跨文件 + multi-root | Case 2, 3, 9 |
| 08 | index-lifecycle | watcher / debounce / rebuild | — |
| 09 | cache-persistence | `Library/` 缓存冷启动 | — |
| 10 | document-symbols | Ctrl+Shift+O | Case 12 |
| 11 | chain-lookup | struct 成员 F12 L1-L3 | Case 10 |
| 12 | macro-definitions | F12 跳 `#define` | Case 11 |
| 13 | find-references | Shift+F12 + Packages 开关 | Case 13, 14 |

依赖：01 → 02 → 03 → 04 → {05, 06} → 07 → {08, 09, 10, 11, 12, 13}。

## Review 摘要（不要跳过）

详见 `docs/superpowers/plans/REVIEW-2026-05-22.md`。**实施前**至少要消化以下事实：

- 有 **10 个 Blocker**，其中 4 个（B3 / B4 / B5 / B7）是 plan 间签名 / 类型 / 文件路径不一致，纯属起草疏漏；2 个（B6 / B7）是 test-electron 套路写错，会让大半集成测试跑空；4 个（B8 / B9 / B10 / B1+B2）是对 Unity 包系统的理解不足或测试 stub。
- tree-sitter-hlsl 节点名是猜的（R1）；建议在 Plan 03 实施前做 30 分钟 spike：拉 wasm、跑 `tree.rootNode.toString()`，把真实节点名 patch 回 Plan 03。
- Performance 三连（R6 / R7 / R8）会让 spec §8.3 的冷启动/单文件预算几乎肯定爆掉；plan 阶段需要补一段 "concurrency model"。

## 下一步路线图（按优先级）

**先做：**

1. **修订轮 A — 一致性 + 测试基础设施**
   推荐用 `superpowers:writing-plans` skill（或直接手改）按 REVIEW 的 B3 / B4 / B5 / B6 / B7 把 plan 文档统一修订一遍。这些都是机械工作，可以在不跑代码的前提下完成。
2. **R1 spike**
   ```bash
   cd <tmp dir>
   git clone https://github.com/tree-sitter-grammars/tree-sitter-hlsl
   cd tree-sitter-hlsl && npx tree-sitter build --wasm
   # 写一个 20 行 Node 脚本：parse 一段典型 HLSL，把 rootNode.toString() 打出来
   ```
   拿到真实节点名后把 Plan 03 的 collector 代码 patch 一遍。
3. **修订轮 B — Unity 包系统**
   研究 `Packages/packages-lock.json` 的 `source` 字段在 embedded / builtin / registry / git / local 几种状态下的真实物理路径，把 Plan 07 的 `resolvePackagePhysicalPath` 改对；或把 builtin / git / scoped registry 标 P2，缩小 MVP 范围。

**再做：**

4. **正式实施 Plan 01**（脚手架）。
   完成判定：`npm run build` 零错、`npm test`（vitest handshake + test-electron activation）全过、按 F5 能看到 `UnityShaderNav: ready` 状态栏。
5. 顺次执行 Plan 02 → 07，达到 MVP。
6. 之后按需推进 Plan 08-13。

## 实施时的执行方式

两选一：

- **Subagent-driven（推荐）**：用 `superpowers:subagent-driven-development` skill，每个 Task 派一个 fresh subagent，两段式审核。适合 plan 已修订干净的情况。
- **Inline**：用 `superpowers:executing-plans` skill，主会话里按批次推进，带 checkpoint。适合需要边做边改 plan 的情况。

如果继承本会话上下文做实施，主线已经清楚；如果是新机器/新会话，**第一件事先读 REVIEW**，再决定是先修订 plan 还是先做 R1 spike。

## 关键命令速查

```bash
# 看 plan 索引
$EDITOR docs/superpowers/plans/README.md

# 看 review
$EDITOR docs/superpowers/plans/REVIEW-2026-05-22.md

# 查 spec 某节
grep -n "^## " UnityShaderNav_Spec.md

# 查依赖关系
sed -n '/^## 依赖图/,/^##/p' docs/superpowers/plans/README.md
```

## 上下文流转

如果切机器后想恢复"我们刚谈到哪了"：

- 当前讨论的最后状态：用户要求"先写一个 handoff 然后把 review 结果写在本地，按照分类 commit 并 push"。**本文档 + REVIEW 文档 + 三个 commit 就是这个动作的产出。**
- 未决问题：还没决定"是先修订 plan 还是先做 R1 spike 再修订"。新会话里和用户对齐。
- 用户角色：技术 lead / 架构者；不需要逐步指导，习惯让 AI 自主决策然后纠偏。
