# UnityShaderNav Plans — Review (2026-05-22)

两轮独立 review 的合并去重清单。仓库还没有代码，全部是 plan 文档层面的问题。所有问题都指明了**具体的 plan + 任务编号**，以便实施前修订。

- **CR** = `superpowers:code-reviewer` 子代理
- **Codex** = `codex:codex-rescue` 子代理
- **双方** = 两轮独立指出同一个问题

Source plans: `docs/superpowers/plans/2026-05-22-{01..13}-*.md`

---

## 🔴 Blockers — 必须改 plan 才能开始实施（10 项）

| # | 问题 | 来源 | 涉及 plan |
|---|---|---|---|
| B1 | Plan 07 Task 10 多 root 测试是 stub：`projectB` fixture 没建、断言写成注释 `// ...`；Spec §10 Case 9 实质未被覆盖 | CR | 07 |
| B2 | Plan 04 Task 8 / Plan 07 Task 10 多个集成测试半截写完（`// ... 类似上面`、`// ... 详见 fixture 设计`） | CR | 04, 07 |
| B3 | Plan 02 `ShaderLabStructureNode`（递归同构）↔ Plan 10 `ShaderLabStructureLite`（手写分层）形状不一致，靠 `structure as any` 强转 | CR | 02, 10 |
| B4 | Plan 11 Task 3 `SymbolEntry.initCallTarget` 字段未加进 `shared/src/symbols.ts`；collector 也没有产出该字段的代码——L3b 是死代码 | CR | 03, 11 |
| B5 | `resolveDefinition` / `registerDocuments` / `indexFile` / `Workspace.bootstrap` 在 plan 04→05→07→09 之间多次改签名，调用方未同步更新 | CR | 04, 05, 07, 09 |
| B6 | `runTests()` 没传 `launchArgs` 打开 fixture 项目目录 → test-electron 跑起来没 workspace folder，Plan 07 后 `WorkspaceManager` 拿不到 workspace，F12 集成测试全返回 null | **Codex** | 01, 04, 05, 06, 07 |
| B7 | Plan 01 `tests/runTest.ts` 指向 `./client/suite` 但 file structure 中没建 `tests/client/suite/index.ts`，且没有 `tests/tsconfig.json` —— 跑不起来 | **Codex** | 01 |
| B8 | builtin Unity 包实际住在 Editor 安装目录（`{Editor}/Data/Resources/PackageManager/BuiltInPackages/`），不在 `Library/PackageCache/`；Plan 07 `resolvePackagePhysicalPath` 的 `builtin` 分支映射错 | 双方 | 07 |
| B9 | `registry` 包路径写成 `name@<hash>`，Unity 实际是 `name@<version>`；同时 Git / local / embedded 缺 `?path=`、`revision`、`git+file:`、绝对 `file:` 等多种合法形态 | **Codex** | 07 |
| B10 | Plan 09 缓存仅按 `(mtime, size)` 校验，未把 settings hash、macro table、WASM grammar 版本写入 manifest；grammar 升级后会复用过期索引 | **Codex** | 09 |

---

## 🟡 Risks — 强烈建议 plan 阶段就修（9 项）

| # | 问题 | 来源 | 涉及 plan |
|---|---|---|---|
| R1 | tree-sitter-hlsl 节点名是猜的；`function_declaration` 大概率不存在（实际是 `declaration` + `function_declarator`）。建议加 Task 0：从 vendored WASM 的 `node-types.json` 反向校准节点名 | 双方 | 03 |
| R2 | Plan 03 Task 2 的 WASM 下载 URL 不存在 release artifact；fallback 需 emscripten 工具链。Task 跑完很可能根本没有可用 `tree-sitter-hlsl.wasm` | CR | 03 |
| R3 | Plan 10 `buildDocumentSymbols` 嵌套算法 `passNodes[0]?.children` 把 Pass 折叠成自己的子级，与 Plan 02 的树形结构互不兼容；作者自己标 "fiddly；测试驱动调试" | CR | 10 |
| R4 | Plan 03 Task 8 注释说 "声明位置也算 reference, plan 13 处理"，但 Plan 13 从未做这个过滤——结果 Shift+F12 会把定义点当成引用列出 | CR | 03, 13 |
| R5 | Plan 06 `existsCaseSensitive` 在 macOS / Windows 上语义反了：`fs.access` 默认大小写不敏感，先 access 再 readdir 的二段式验证在 mac 不可靠；CI 在 mac/linux 跑会得到不同结果 | CR | 06 |
| R6 | Plan 09 cold-start cache 加载是 O(n) 串行 `fs.stat()`；2000+ 文件下肉眼可见超 spec §8.3 的 "<4s" 预算 | 双方 | 09 |
| R7 | Plan 09 "每次 applyChanges/rebuild 后 persist" 会把整个 index JSON 重写到磁盘；Packages 在缓存中时单文件保存 < 100ms 必爆 | **Codex** | 09 |
| R8 | Plan 07 `fullScan()` 是串行 read + parse + AST walk，无 bounded concurrency、无 worker threads；URP / HDRP 量级冷启动会超 spec §8.3 预算且阻塞 LSP event loop | **Codex** | 07 |
| R9 | Plan 08 `RequestSuspender` 与 rebuild 的接线只有散文描述、没代码；rebuild 期间多 F12 排队、释放后并发涌出，无取消、无背压 | 双方 | 08 |

---

## 🟢 Polish — 实施时再优化即可（7 项）

| # | 问题 | 来源 | 涉及 plan |
|---|---|---|---|
| P1 | Plan 05 `matchDeclarationCall` 只用 `params[0]` 这种单 capture；定义中允许的多 capture 模式被静默丢失 | CR | 05 |
| P2 | Plan 05 `matchPragmaLine` 用 `indexOf(captured, ...)` 找捕获位置；`#pragma kernel kernel` 这种 captured 与 directive 同名时定位错 | CR | 05 |
| P3 | Plan 08 用 `this.store.clear?.()` 调用 `IndexStore.clear()`，但该方法在前面任何 plan 都没正式定义；optional chaining 是补丁式写法 | CR | 07, 08 |
| P4 | Spec §10 Case 7（`.compute` + `#pragma kernel`）Plan 05 acceptance 写了但没建 `.compute` fixture，集成验证缺手段 | CR | 05 |
| P5 | Plan 02 `structureScanner` Pass-Name 测试只覆盖紧接 Pass 的写法，没覆盖 `Tags/Cull/Name` 顺序变化 | CR | 02 |
| P6 | Plan 09 cache 是明文 JSON 未压缩，HDRP 量级下 `index.json` 会很大；ADR-0004 不强制但值得标 P2 | CR | 09 |
| P7 | Plan 07/08/09 多处跨 `await` 操作 `IndexStore` / `GlobalSymbolIndex`；JS 单线程不能保证跨 `await` 的索引快照一致——需要 snapshot / 操作队列模型 | **Codex** | 07, 08, 09 |

---

## ✅ 两位都确认设计正确，不必再纠结

- **多候选 Peek（ADR-0001）** 落地正确：`Map<string, SymbolEntry[]>`、不去重、F12 返回数组（Plan 04 / 07）
- **macro 白名单（ADR-0003）** 方向正确：没去做真宏展开，认知边界清楚
- **Include 搜索优先级 + case-insensitive fallback warning** 符合真实 Unity 跨平台问题；把 Packages 延后到 Plan 07 让边界干净（Plan 06）
- **`onDocumentSymbol` / `onReferences` 协议签名**正确；`includeDeclaration` 被尊重（Plan 10 / 13）
- **Plan 03 对 tree-sitter 节点名的不确定性是承认而不是装稳定** —— 风险处理姿态对

---

## Spec §10 验收覆盖审计

| Case | 描述 | 计划 | 现状 |
|---|---|---|---|
| 1 | 同 .shader 内函数 F12 | 04 | OK |
| 2 | 跨 .hlsl 文件 F12 | 07 | OK |
| 3 | F12 到 Packages 中的定义 | 07 | OK |
| 4 | F12 在 `#include` 路径上 | 06 | OK |
| 5 | F12 在 `TEXTURE2D` 声明变量上 | 05 | OK |
| 6 | F12 在 `#pragma vertex vert` 的 `vert` 上 | 05 | OK |
| 7 | F12 在 `.compute` 的 `#pragma kernel CSMain` 上 | 05 | ⚠️ 无 `.compute` fixture（P4） |
| 8 | F12 在函数参数 identifier 上 | 04 | OK |
| 9 | multi-root 隔离 | 07 | 🔴 测试是 stub（B1） |
| 10 | F12 在 struct 成员 `.positionWS` | 11 | ⚠️ L3b 是死代码（B4） |
| 11 | F12 在宏使用上跳到 `#define` | 12 | OK |
| 12 | Ctrl+Shift+O 显示大纲 | 10 | ⚠️ 嵌套算法 fiddly（R3） |
| 13 | Shift+F12 列引用（user files） | 13 | ⚠️ 含声明点（R4） |
| 14 | Shift+F12 包含 Packages 的开关 | 13 | OK |

---

## 推荐的修订顺序

1. **B6 + B7 + B3 + B5**：合并发一轮"测试基础设施 + 跨 plan 一致性"修订；这些是机械性的、影响面最广。
2. **R1 spike**：拉 wasm、跑 `tree.rootNode.toString()` 把真实节点名 patch 回 Plan 03；只要 30 分钟，但能消除 Plan 03 大量返工风险。
3. **B8 + B9 + B10**：研究 Unity PackageManager / packages-lock.json 真实 schema 后改 Plan 07 / 09；或显式把"builtin / git / scoped registry"标 P2，收紧 MVP 范围。
4. **B1 + B2**：把 stub 测试补完。
5. **R6 + R7 + R8 + P7**：性能 + 并发模型 plan 阶段需要补一段 "concurrency model"。
6. **R3 + R4 + R5**：单点修复。
7. **R9 + R2**：plan 阶段加少量代码即可。

完成 1-3 之后即可启动实施；剩余项可在执行对应 plan 时再增量改 plan。
