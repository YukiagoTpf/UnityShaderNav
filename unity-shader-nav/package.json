{
  "name": "unity-shader-nav-monorepo",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "watch": "npm run watch --workspaces --if-present",
    "test": "npm run build && tsc -p tests/tsconfig.json && node tests/out/runTest.js && npm run test --workspaces --if-present",
    "clean": "rimraf client/out server/out shared/out client/tsconfig.tsbuildinfo server/tsconfig.tsbuildinfo shared/tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "esbuild": "^0.28.0",
    "rimraf": "^6.1.3",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
